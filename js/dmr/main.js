define(["dojo/_base/kernel"], function(dojo) {
	// module:
	//		dmr/main

	/*=====
	return {
		// summary:
		//		The dmr package main module; dmr package is somewhat unusual in that the main module currently just provides an empty object.
		//		Apps should require modules from the dmr packages directly, rather than loading this module.
	};
	=====*/

	return dojo.dmr;
});