define(["dojo/_base/kernel"], function(dojo) {
	// module:
	//		cv/main

	/*=====
	return {
		// summary:
		//		The cv package main module; cv package is somewhat unusual in that the main module currently just provides an empty object.
		//		Apps should require modules from the cv packages directly, rather than loading this module.
	};
	=====*/

	return dojo.cv;
});